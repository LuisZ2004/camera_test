{
  "name": "your-project",
  "version": "1.0.0",
  "description": "...",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs": "jsdoc -c jsdoc.conf.json -r -d docs"
  },
  "devDependencies": {
    "jsdoc": "^4.0.0" 
  }
}